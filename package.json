{
  "name": "helping-hand",
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt && python manage.py collectstatic --noinput --clear --settings=helpinghand.test_settings"
  }
}
